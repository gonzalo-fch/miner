version: "3.9"

services:

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  miner:
    build: ./miner
    depends_on:
      - redis

  visualizer:
    build: ./visualizer
    ports:
      - "8501:8501"
    depends_on:
      - redis